# v0.2.0 (draft, pre-tag)

Status: release notes prepared, Git tag not yet published.

## Included scope

- Phase B-F orchestrator runtime implementation
- SQLite migrations and persistence primitives
- `/execute-plan` create + same-thread approval gate
- Canonical dispatch engine with routing, PR/CI, screenshot requirements
- Watchdog worker tick + retry/escalation logic
- Acceptance script with explicit modes (`--mock`, `--real-local`, `--real-discord`)

## Tagging note

Before treating this as a finalized release, create and push the release tag:

```bash
git tag -a v0.2.0 -m "v0.2.0"
git push origin v0.2.0
```
